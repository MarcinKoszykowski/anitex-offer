import styled from 'styled-components';
import { white } from 'styled/colors';

const Text = styled.p`
  font-size: 1.8rem;
  color: ${white};
  text-transform: uppercase;
  letter-spacing: 2px;
  font-weight: 600;
`;

export default Text;
